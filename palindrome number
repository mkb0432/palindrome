#include<bits/stdc++.h>
using namespace std;
int main()
{
int n;
cin>>n;
int d;int m,rev=0;
while(n>0)
{
d=n%10;
rev= rev*10+d;
n=n/10;
}
if(rev==m)
cou<<"Palindrome";
else
cout<<"Not Palindrome";
return 0;
}
